#include <iostream>  // C++ header, no .h extension

int OpenFile() {       // Function in PascalCase
  int max_count = 10;  // variable in snake_case
  std::cout << max_count << std::endl;
  return max_count;
}