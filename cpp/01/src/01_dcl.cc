#include <iostream>

int main() {                   // C++: variables can be declared when needed
  for (int i = 0; i < 3; ++i)  // declared inside loop
    std::cout << i << std::endl;
  return 0;
}
