enum escapes {
    BELL = '\a',
    BACKSPACE = '\b',
    TAB = '\t',
    NEWLINE = '\n',
    VTAB = '\v',
    RETURN = '\r'
};

enum months { JAN = 1, FEB, MAR }; /* FEB is 2, MAR is 3 */

enum boolean { NO, YES };

enum boolean condition = YES; /* you can use enum tags in this case */
